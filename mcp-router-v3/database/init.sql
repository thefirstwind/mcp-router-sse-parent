-- 创建数据库
CREATE DATABASE IF NOT EXISTS `mcp-bridge` CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 使用数据库
USE `mcp-bridge`;

-- 创建用户（如果不存在）
-- CREATE USER IF NOT EXISTS 'root'@'localhost' IDENTIFIED BY 'hot365fm';
-- GRANT ALL PRIVILEGES ON `mcp-bridge`.* TO 'root'@'localhost';
-- FLUSH PRIVILEGES;